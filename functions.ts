function multiplicar(a: number, b: number): number {
  return a * b;
}

function ola(nome: string): string {
  return "Olá " + nome;
}

const resultado = multiplicar(5, 3); 
const saudacao = ola("João"); 

console.log(resultado)
console.log(saudacao)